// Re-export Prisma types after generation
// Run `npx prisma generate` first if you see import errors here
export type {
  User,
  StudentProfile,
  Curriculum,
  Week,
  Resource,
  Task,
  SkillAssessment,
  ProgressEntry,
  ExperienceLevel,
  PreferredLanguage,
  CurriculumStatus,
  ResourceType,
  TaskType,
} from '@prisma/client';
